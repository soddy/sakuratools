window.addEventListener('load', function(){
    sakura.debug = false;
    var stage = window.stage = new sakura.Stage('canvas', 750, 1448, 60, sakura.stageScaleMode.FIXED_BOTH, true);
    stage.addEventListener('loaded', function(e){

    });
});